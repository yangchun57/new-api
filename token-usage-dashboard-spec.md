# API Key 用量监控台 — 功能清单

> 来源页面：`https://token01.hnabc.cn/`
> 用途：面向 API Key 持有者的 Token 用量、缓存命中、请求数实时查询页面。
> 数据延迟：1-4 小时（页面顶部明确标注 "Live · 延迟 1-4 小时"）。

---

## 一、页面整体结构

页面为单页（SPA），纵向布局，分为以下 3 个区域：

```
┌─────────────────────────────────────────────────┐
│  Header：标题 + 副标题 + 实时状态标识            │
├─────────────────────────────────────────────────┤
│  筛选区（Filter Bar）：查询条件 + 操作按钮       │
├─────────────────────────────────────────────────┤
│  用量趋势（Charts）：可视化图表区域              │
├─────────────────────────────────────────────────┤
│  用量明细（Data Table）：数据表格                │
├─────────────────────────────────────────────────┤
│  Footer：数据来源说明                            │
└─────────────────────────────────────────────────┘
```

---

## 二、功能模块清单

### 模块 1：页头（Header）

| 元素 | 说明 |
|------|------|
| 主标题 | "API Key 用量监控台" |
| 副标题 | "API Key Token 用量 · 缓存命中 · 请求数实时查询" |
| 实时状态徽标 | "Live · 延迟 1-4 小时"（带 Live 小圆点，绿色脉冲动效） |

**功能要点：**
- 清晰告知页面用途和数据时效性。
- "延迟 1-4 小时"是重要提示，避免用户误判为秒级实时。

---

### 模块 2：筛选区（Filter Bar）

用户通过以下筛选条件组合查询数据：

| 筛选项 | 控件类型 | 可选项 / 说明 |
|--------|----------|---------------|
| 起始日期 | 日期选择器 | 格式 `MM/DD/YYYY`，默认值为前一天 |
| 结束日期 | 日期选择器 | 格式 `MM/DD/YYYY`，默认值为当天 |
| 粒度 | 单选切换（Radio/Tabs） | **按小时** / **按天**（默认按小时） |
| 起始小时 | 下拉选择（Select） | 仅"按小时"粒度时生效；选项：`00:00（全部）`、`12:00`–`23:00`；默认 `00:00（全部）` |
| 模型筛选 | 下拉选择（Select） | 默认"全部模型"；选项为该 API Key 调用过的所有模型列表（如 gpt-4o、gpt-4o-mini 等） |

**操作按钮：**

| 按钮 | 行为 |
|------|------|
| 查询 | 根据当前筛选条件请求后端数据，刷新图表和表格 |
| 导出 CSV | 将当前筛选结果导出为 CSV 文件下载（文件名/内容包含日期、模型、Token 明细等） |

**交互逻辑：**
1. 粒度切换到"按天"时，"起始小时"选项应被禁用或隐藏。
2. 起始日期不能晚于结束日期（日期区间校验）。
3. 日期范围应做合理上限（例如单次最多查 31 天或 90 天，视后端能力而定）。
4. 切换筛选条件后需点击"查询"才重新拉取数据（非实时响应输入变化）。

---

### 模块 3：用量趋势（Charts）

图表区域（标题"用量趋势"），用于可视化展示时间序列数据。结合表头字段推断应包含以下图表（原页面为 JS 渲染，具体图表类型需实际访问确认，下面是合理推断）：

| 图表 | 维度 | 指标 | 建议图表类型 |
|------|------|------|-------------|
| Token 用量趋势 | X 轴：时间（小时/天）；系列：模型 | 输入 Token、输出 Token、总 Token | 堆叠折线图 / 堆叠面积图 |
| 缓存命中率趋势 | X 轴：时间 | 缓存命中 / 缓存未命中 | 堆叠柱状图或双线折线图 |
| 请求数趋势 | X 轴：时间；系列：模型 | 请求数 | 折线图 / 柱状图 |
| 金额趋势 | X 轴：时间 | 金额（元） | 面积图 |

**图表功能要素：**
- 支持鼠标 Hover 显示 Tooltip（具体时间点、各模型数值）。
- 支持图例（Legend）点击切换显示/隐藏某模型。
- 时间轴根据"粒度"自适应：按小时显示 HH:00，按天显示 MM/DD。
- 支持时间范围缩放/平移（可选增强）。
- 空数据状态展示"暂无数据"占位。

---

### 模块 4：用量明细（Data Table）

数据表格区域（标题"用量明细"），以表格形式列出按维度聚合后的原始数据。

**表头列：**

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 日期 | date | string | `YYYY-MM-DD` |
| 小时 | hour | string | `HH:00`；按天粒度时此列为空或显示"-" |
| 模型 | model | string | 模型标识，如 `gpt-4o-2024-08-06`、`gpt-4o-mini` |
| 输入 Token | prompt_tokens | number | 非缓存的输入 Token 数（或总输入，需结合缓存拆分逻辑） |
| 缓存命中 | cached_tokens | number | 缓存命中的 Token 数（通常对应 prompt caching，计费有折扣） |
| 缓存未命中 | uncached_tokens | number | 缓存未命中的 Token 数 |
| 输出 Token | completion_tokens | number | 模型输出 Token 数 |
| 总 Token | total_tokens | number | = 输入 + 缓存命中 + 输出（或按业务口径） |
| 请求数 | requests | number | 该时间粒度下该模型的 API 请求次数 |
| 金额(元) | cost | number | 按模型单价计算的费用，保留 2-4 位小数 |

**表格功能要素：**
- 默认排序：按日期+小时倒序（最新在上）。
- 支持按列排序（点击表头切换升序/降序）。
- 数字列右对齐；金额列按货币格式显示（千分位、保留小数位）。
- 支持分页（数据量大时）或虚拟滚动。
- 支持每行/每模型的小计、全表合计行（可选）。
- "导出 CSV"按钮导出的内容应与表格当前筛选/排序一致。
- 空状态：无数据时显示"当前筛选条件下暂无数据"。

---

### 模块 5：页脚说明（Footer）

```
数据来源：OpenAPI · 用量数据按小时更新，存在 1-4 小时延迟
```

- 固定在表格下方，用小字（次要文本色）展示。
- 说明数据来源（OpenAPI）和延迟特性，与顶部徽标呼应。

---

## 三、接口需求（推断）

页面需要 1 个核心查询接口（参考 OpenAI Usage API 风格）：

### GET /api/usage

**Query 参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| start_date | string (YYYY-MM-DD) | 是 | 起始日期 |
| end_date | string (YYYY-MM-DD) | 是 | 结束日期 |
| granularity | string | 是 | `hour` \| `day` |
| start_hour | number (0-23) | 否 | 按小时粒度时，限定起始小时（可选，用于只看半天） |
| model | string | 否 | 模型名筛选，不传则查全部 |
| api_key_id | string | 视情况 | 若页面需支持多 Key 切换；若单 Key 则从登录态获取 |

**响应结构（建议）：**

```json
{
  "code": 0,
  "data": {
    "granularity": "hour",
    "start_date": "2026-08-31",
    "end_date": "2026-09-01",
    "models": ["gpt-4o", "gpt-4o-mini"],
    "buckets": [
      {
        "date": "2026-09-01",
        "hour": 14,
        "model": "gpt-4o",
        "input_tokens": 12345,
        "cached_tokens": 67890,
        "uncached_tokens": 12345,
        "output_tokens": 54321,
        "total_tokens": 134556,
        "requests": 87,
        "cost": 1.2345
      }
    ]
  }
}
```

**CSV 导出接口：** 可复用同一查询参数，返回 `text/csv` 流；或前端基于已查询的 buckets 直接生成 CSV（数据量不大时更简单）。

---

## 四、页面交互流程

```
页面加载
  │
  ▼
初始化默认筛选（起=昨天，止=今天，粒度=按小时，小时=全部，模型=全部）
  │
  ▼
自动触发一次"查询"（或等待用户点击）
  │
  ▼
展示 Loading 状态 → 请求接口
  │
  ├─ 成功 → 渲染图表 + 渲染表格
  │
  └─ 失败 → 展示错误提示（Toast / 空态错误信息）
  │
  ▼
用户修改筛选条件 → 点击"查询" → 重新拉取
用户点击"导出 CSV" → 下载文件
```

---

## 五、非功能性需求

| 项目 | 要求 |
|------|------|
| 鉴权 | 页面必须在登录态（或携带 API Key 身份）下访问；只能看自己 Key 的数据 |
| 响应式 | 筛选区在窄屏下折行；图表在移动端可横向滚动；表格在移动端可横向滚动 |
| 性能 | 单月按小时粒度数据量约 30×24×N模型 ≈ 数千行，需前端聚合/分页处理 |
| 数字格式化 | 大数字使用千分位；金额保留 4 位小数；Token 数整数 |
| 时区 | 时间展示应使用用户本地时区或后端统一时区（建议后端返回的 hour 已按目标时区聚合） |
| 可访问性 | 筛选控件需有 label；表格需有正确的 `<th scope>`；图表需提供数据下载替代（CSV 已覆盖） |
| 空态/错态 | 网络错误、无数据、日期非法均有明确提示 |

---

## 六、技术选型建议（供参考）

- **框架**：React + TypeScript（与现有项目 `web/` 技术栈一致）。
- **UI 组件**：现有项目已使用 Base UI + Tailwind，可复用 DatePicker、Select、Button、Table。
- **图表库**：推荐 **ECharts**（功能全、中文文档好、支持堆叠/tooltip/legend 完善）或 **Recharts**（与 React 集成更轻量）。
- **日期处理**：dayjs（已广泛使用）。
- **CSV 导出**：前端拼装后用 `Blob` + `URL.createObjectURL` 触发下载，无需后端接口。
- **请求**：复用项目中已有的 axios/fetch 封装。

---

## 七、开发 Checklist

- [ ] 页面路由与菜单入口
- [ ] 筛选区表单（日期、粒度、小时、模型）
- [ ] 查询/导出按钮与交互校验
- [ ] 接口对接（类型定义、Loading、错误处理）
- [ ] 用量趋势图表（Token/缓存/请求数/金额）
- [ ] 用量明细表格（10 列、排序、分页、空态）
- [ ] CSV 导出功能
- [ ] 页头 Live 徽标 + 页脚数据来源说明
- [ ] 响应式适配
- [ ] 空数据 / 错误 / 加载中三种状态
- [ ] 数字、金额格式化
